# Get two numbers (integer or floating-point) from the user
A = float(input("Enter the first number (A): "))
B = float(input("Enter the second number (B): "))

# Calculate the sum
sum_result = A + B

# Print the sum
print("Sum =", sum_result)
